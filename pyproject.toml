[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.python]
# 禁止生成 .pyc 文件和 __pycache__ 目录
write-bytecode = false

[tool.pytest.ini_options]
addopts = ["-p", "no:cacheprovider"]
